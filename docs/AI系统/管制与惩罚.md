# AI 管制与惩罚系统

## 核心思想

AI 持续监控当前活动与主线目标的对齐度。当主人偏离主线，触发惩罚升级链。

---

## 惩罚升级链

| 等级 | 方式 | 示例 |
|------|------|------|
| 1. 轻度提醒 | 语音 + 通知弹窗 | "你已经刷了 20 分钟微博了，要回到主线吗？" |
| 2. 中度干预 | 全屏推送，不可忽略 | 向所有设备推送待办任务，遮住屏幕 |
| 3. 重度惩罚 | 限制应用使用时间 | 联动设备管理工具，限制特定应用 |
| 4. 终极管制 | 自动退出/拦截 | 检测到银行/转账界面时自动退出应用 |

---

## 场景举例

- 主人最近没有继续 NotebookLM + Anki 流程学习代码 → 系统检测到学习习惯中断 → 降低「技能」维度经验 → 推送学习任务到所有设备 → 不完成就持续遮屏提醒
- 主人连续刷社交媒体超过阈值 → 语音警告 → 全屏推送 → 限制该应用使用
- 主人试图冲动消费 → 检测到银行/支付界面 → 自动退出 + 弹出「系统不允许此操作」

---

## 任务自动分配

- AI 不只是分析你做了什么，而是**告诉你该做什么**
- 基于主线目标 + 当前进度 + 时间规律，自动生成每日/每周任务清单
- 任务有优先级、截止时间、关联的动机维度
- 未完成的任务自动升级惩罚等级

---

## 语音能力

语音不局限于总结播报，而是 AI 的常态沟通方式之一。调用 Fish TTS API，固定音色，让 AI 有一个「声音身份」。

**场景举例：**
- 总结播报："过去一小时你在推进 SoloAgent 的面板开发，进展不错"
- 实时提醒："你已经刷了 20 分钟微博了，要回到主线吗？"
- 方向建议："接下来可以考虑先把通知系统的 bug 修完"
- 鼓励："连续专注了两小时，休息一下吧"

不用每句话都语音，AI 自己判断什么时候值得「开口说」。

---

## AI 总结后的对话确认

每次 AI 完成活动总结后，可以让用户确认准确性：

| 方案 | 描述 |
|------|------|
| A. 强制弹窗 | 适合高频互动用户 |
| B. 菜单栏红点 | 用户自选时机 review（不打断） |
| C. 积攒后批量 review | 等用户主动打开面板时一起看 |

交互期间暂停截屏 + 时间线记录为「与系统交互」。

---

## 多设备推送（待实现）

- macOS → iOS/iPadOS 推送（APNs 或自建推送通道）
- 全屏遮罩式通知——不是普通 banner，必须响应才能关闭
- 后期考虑 Android / Windows 端扩展

## 设备管制（待实现）

- macOS: Screen Time API 或自定义 kext 限制应用
- iOS: MDM 配置文件或 Screen Time 家长控制
- 银行/支付管制: 检测到相关界面（OCR 或窗口标题匹配）→ kill 进程或切屏

---

## 状态

核心方向，待设计详细机制
