<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- LAN 同步：UDP multicast 广播给 desktop SyncDiscovery 自动发现 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- 感知层：app 使用情况采集。用户需在设置→应用→特殊访问权限→使用情况访问 中授权。 -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- 感知层：开机自动启动 — 记录 boot 事件 + 自动起 Service 监听 SCREEN_OFF/ON -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- App 自更新：从 OSS 下载 APK 后拉系统安装器；用户首次需在系统设置允许"未知来源" -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:screenOrientation="portrait"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- 感知层：前台窗口切换监听 (Phase 2)。用户需在 设置→辅助功能 中手动启用。 -->
      <service
        android:name=".perception.SlsAccessibilityService"
        android:label="Solo Leveling · 活动感知"
        android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
        android:exported="true">
        <intent-filter>
          <action android:name="android.accessibilityservice.AccessibilityService" />
        </intent-filter>
        <meta-data
          android:name="android.accessibilityservice"
          android:resource="@xml/sls_accessibility_service" />
      </service>

      <!-- 自更新下载 APK 后拉系统安装器需要 FileProvider 共享文件 URI -->
      <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/sls_file_provider_paths" />
      </provider>

      <!-- 开机 / 关机事件 receiver：必须 manifest 静态注册。SCREEN_ON/OFF 等
           不允许 manifest 注册（Android O+ implicit broadcast 限制），那部分在
           SlsAccessibilityService 动态注册；BOOT_COMPLETED 是 explicit，可以。 -->
      <receiver
        android:name=".perception.PowerStateReceiver"
        android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
          <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
          <action android:name="android.intent.action.ACTION_SHUTDOWN" />
        </intent-filter>
      </receiver>
    </application>
</manifest>
